{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pay.id }}</td>
            </tr>
            <tr>
                <th>NomPays</th>
                <td>{{ pay.nomPays }}</td>
            </tr>
            <tr>
                <th>CodePays</th>
                <td>{{ pay.codePays }}</td>
            </tr>
            <tr>
                <th>CodeTelPays</th>
                <td>{{ pay.codeTelPays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pays_index') }}">back to list</a>

    <a href="{{ path('pays_edit', {'id': pay.id}) }}">edit</a>

    {{ include('pays/_delete_form.html.twig') }}
{% endblock %}
